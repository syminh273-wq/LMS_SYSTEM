info:
  name: Retrieve Consumer
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/consumer/account/{{consumerUid}}"
  auth: bearer

auth:bearer {
  token: "{{consumer_access_token}}"
}
