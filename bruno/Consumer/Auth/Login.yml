info:
  name: Consumer Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/v1/consumer/account/login/"
  body:
    type: json
    data: |-
      {
        "email": "{{email}}",
        "password": "{{password}}"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        if (data.access) {
          bru.setEnvVar("consumer_access_token", data.access);
        }
