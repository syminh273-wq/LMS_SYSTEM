info:
  name: Consumer Register
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/consumer/account/register/"
  body:
    type: json
    data: |-
      {
        "email": "{{email}}",
        "password": "{{password}}"
      }
