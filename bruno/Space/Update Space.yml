info:
  name: Update Space
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/space/account/{{spaceUid}}"
  auth: bearer

auth:bearer {
  token: "{{space_access_token}}"
}
  body:
    type: json
    data: |-
      {
        "name": "Updated Name"
      }
