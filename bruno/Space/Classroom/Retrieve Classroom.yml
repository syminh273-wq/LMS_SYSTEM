info:
  name: Retrieve Classroom
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/space/course/classrooms/{{classroomUid}}/"
  auth: bearer

auth:bearer {
  token: "{{space_access_token}}"
}
