info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/account/register"
  body:
    type: json
    data: |-
      {
        "email": "{{email}}",
        "password": "{{password}}"
      }

runtime:
  variables:
    - name: email
      value: minh.ho@chek.vn
    - name: password
      value: "@Minh1182004"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
